[project]
name = "report"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "altair>=5.5.0",
    "duckdb>=1.1.3",
    "streamlit>=1.41.1",
]

[dependency-groups]
dev = [
    "ruff>=0.8.4",
]
